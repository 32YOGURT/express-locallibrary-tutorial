extends layout 

block content
    h1= title 
    if genrelist.length 
        ul 
            each genre in genrelist 
                li 
                    a(href=genre.url) #{genre.name} 

    else 
        p There are no genres.
